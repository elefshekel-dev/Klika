# רסיסים — הנחיות לשיחות עתידיות

מסמך זה מגדיר את מה שאסור להפוך במוצר. קראי אותו לפני כל שינוי.

## מה זה
מחברת רסיסים ספרותית. **לא אפליקציית מסמכים** — המשתמשת כותבת עשרות ומאות
טקסטים קצרים ומקוטעים (שורה, בית, פסקה, רעיון), לא חמישה מסמכים ארוכים.
הכתיבה בעברית — RTL מלא הוא דרישת ליבה.

## שלושת עקרונות המוצר (אסור להפוך)
1. **לכידה בלי חיכוך.** מרעיון לטקסט שמור בפחות משתי שניות. כל שדה חובה הוא
   כישלון. כפתור "+" פותח רסיס חדש עם הסמן בפנים — בלי דיאלוג, בלי בחירת סוג.
2. **מציאה מחדש.** הערך הוא ברסיס שנכתב לפני חצי שנה ונמצא שוב היום. הספרייה
   חשובה יותר מהעורך.
3. **Local-first.** הכל מקומי (IndexedDB דרך Dexie), עובד אופליין לחלוטין.
   הסנכרון (Google Drive) הוא שכבה opt-in מעל, לא תלות. **אין backend.**

## מה לא לבנות (אסור להוסיף)
- אין ייבוא מקבצים חיצוניים. המשתמשת מתחילה מאפס.
- **אין שיתוף, אין קישורים ציבוריים, אין הרשאות, אין מסך התחברות/משתמשים.**
  זה כלי אישי. שיתוף מכוסה דרך ייצוא/העתקת טקסט מעוצב בלבד.
- אין סטטוסים, workflow, או שלבי עריכה.
- אין ספירת יעדים יומיים, streaks, או גיימיפיקציה.
- אין Markdown, אין סרגל עיצוב, אין כפתורי פורמט. טקסט חופשי בלבד.
- אין ספריית UI כבדה. אפליקציה סטטית בלבד.

## החלטות עיצוב שאסור להפוך
- **לרסיס אין שדה חובה מלבד `content`.** אין כותרת חובה, אין תגית חובה, אין
  פרויקט חובה, אין סטטוס. אם אין כותרת — מציגים את השורה הראשונה החתוכה.
- **אין היררכיה.** תגיות ואסופות הן שכבות שטוחות שמונחות מעל, ורסיס יכול לחיות
  בלי אף אחת מהן.
- **שבירות שורה קדושות.** נשמרות בדיוק כפי שהוקלדו — אין auto-format, אין המרת
  שורות ריקות, אין trim על התוכן. בתים מופרדים בשורה ריקה ונשמרים ככה.
  (משתמשים ב-`white-space: pre-wrap` ולא נוגעים בתוכן.)
- **תיוג קורה אחרי הכתיבה, לא לפני.** אין מסך "ניהול תגיות" למלא מראש. אין
  טקסונומיה מוגדרת מראש — התגיות צומחות מהשימוש, שדה חופשי אחד.
- **שמירה אוטומטית מהתו הראשון** (debounce 500ms). רסיס ריק שננטש נמחק בשקט.
- **כיוון אוטומטי לפי תוכן** (RTL/LTR), עם טיפול בעברית-אנגלית מעורבת.
- **נייד הוא הפלטפורמה הראשית.** מעצבים לנייד קודם. אזורי מגע ≥ 44px.
- **קונפליקט סנכרון: לעולם לא לדרוס.** יוצרים עותק ומתריעים. התכנסות לפי
  `updatedAt` + `deviceId`.

## מודל הנתונים (`src/db/types.ts`)
- **Fragment**: `id, content, title?, tagIds[], createdAt, updatedAt, isPinned,
  isDeleted, deviceId`. `content` הוא השדה החובה היחיד.
- **Tag**: `id, name, color, createdAt, updatedAt, useCount, isDeleted, deviceId`.
- **Collection**: `id, name, description, fragmentIds[] (מסודר), createdAt,
  updatedAt, isDeleted, deviceId`. ריבוי חברות (CollectionMembership) ממומש דרך
  `fragmentIds` — אותו רסיס יכול להיות בכמה אסופות. מחיקת אסופה לא מוחקת רסיסים.
- **Version**: `id, fragmentId, content, savedAt, deviceId`. 30 אחרונים לרסיס.
- **Settings**: מקומי, לא מסונכרן.
- כל ישות נושאת `updatedAt` ו-`deviceId`. **אין להכניס הנחות single-user קשיחות
  למודל** — הוא מוכן לריבוי מכשירים.

## כללי סכמה קדושים (Dexie)
- **כל שינוי סכמה חייב לעבור דרך `db.version(N).stores(...).upgrade(...)` חדש,
  עם מיגרציה מפורשת.** יש נתונים אמיתיים בצד השני. לעולם אין לערוך גרסה קיימת.
- `isDeleted`/`isPinned` הם בוליאנים ולכן **אינם ניתנים לאינדוקס** ב-IndexedDB.
  מסננים אותם ב-JS (`src/db/queries.ts`), לא דרך `where`.
- **אין לכתוב בתוך `useLiveQuery`** — זו טרנזקציית קריאה-בלבד (ReadOnlyError).
  קריאות בלבד ב-liveQuery; כתיבות נעשות ב-effects/handlers.

## סנכרון (Google Drive — `src/sync/`)
- opt-in לחלוטין. בלי חיבור, האפליקציה עובדת במלואה. אין backend, אין סוד לקוח.
- אימות דרך Google Identity Services בצד הלקוח, scope `drive.file` בלבד.
- **מקור האמת למיזוג הוא `fragments-data.json`** בתיקייה "רסיסים" ב-Drive.
  קובצי ה-md (`fragments/`, `collections/`) הם גיבוי קריא לאדם, כתיבה בלבד —
  לא קוראים אותם חזרה למיזוג.
- **מיזוג תלת-כיווני** (`merge.ts`, טהור ונבדק) עם מפת בסיס (`BaseMap`) של
  updatedAt מהסנכרון האחרון. קונפליקט אמיתי (שני צדדים שינו מאז הבסיס ותוכן
  שונה) → הגרסה החדשה נשמרת ונוצר **עותק** מהשנייה. לעולם לא דורסים.
- המיזוג דטרמיניסטי (updatedAt ואז deviceId) ולכן מתכנס בין מספר מכשירים.
- מזהה הלקוח מגיע מ-`VITE_GOOGLE_CLIENT_ID` בלבד, לא hardcoded.

## סטאק
React + TypeScript + Vite · Dexie (IndexedDB) · Tailwind · vite-plugin-pwa.
בלי router כבד או UI kit. `@/` = `src/`.

## מבנה
- `src/db/` — סכמה, טיפוסים, שכבת גישה (fragments, tags, collections, versions,
  settings, queries).
- `src/lib/` — עזרי טקסט: `hebrew.ts` (נרמול לחיפוש), `direction.ts` (RTL/LTR),
  `textStats.ts`, `id.ts`, `device.ts`.
- `src/features/` — editor, library, ועוד לפי תחום.
- `src/components/` — רכיבים משותפים (Fab, FragmentCard, icons).
- `src/hooks/` — hooks משותפים.

## שפת הערות
כל הערות הקוד בעברית — שפה אחת עקבית.
